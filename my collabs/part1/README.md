# Практика по SCADA-данным ветроустановки

Состав папки:

- `Wind_Turbine_Tutorial_RU_ML.ipynb` — основной учебный ноутбук;
- `Wind_Turbine_Litvinenko.ipynb` — ноутбук с выполненными заданиями 1-5;
- `Wind_Turbine_Practical_Tasks_RU_ML.ipynb` — копия практического ноутбука;
- `dataset/` — исходные CSV-файлы;
- `requirements.txt` — список библиотек.

## Запуск

### Google Colab

1. Загрузить папку `dataset` так, чтобы путь был `/content/dataset`.
2. Открыть практический ноутбук.
3. В первой технической ячейке при необходимости выполнить установку:

```python
!pip install -q imbalanced-learn lightgbm
```

4. Запустить ячейки сверху вниз.

### Локальный запуск

Установить зависимости:

```bash
python -m pip install -r requirements.txt
```

Дальше открыть ноутбук в Jupyter или VS Code и выбрать ядро из `.venv`.

## Порядок работы

1. Загрузить `scada_data.csv`, `status_data.csv`, `fault_data.csv`.
2. Привести столбцы времени к формату `datetime`.
3. Оставить общий временной период для всех таблиц.
4. Разметить SCADA-записи через `merge_asof`: ближайший отказ ищется в небольшом временном окне.
5. Записи без найденного отказа отметить как `NF`.
6. Убрать лишние признаки: время, мощность, выработку, скорость ветра и служебные столбцы.
7. Подготовить `X` и `y`, затем обучить модели.

## Что смотреть в заданиях

1. На boxplot для `WEC: ava. reactive Power` проверить, выделяется ли класс `EF`.
2. На графике `SMOTE` и `BorderlineSMOTE` сравнить F1-score по каждому классу.
3. На feature importance посмотреть, какие датчики чаще всего использует модель.
4. На графике `StratifiedKFold` vs `TimeSeriesSplit` сравнить, как меняются macro-метрики при временной проверке.
5. На PCA-графике проверить, отделяются ли классы визуально.

## Краткие замечания

`SMOTE` и `BorderlineSMOTE` помогают работать с дисбалансом классов, но не делают редкие классы автоматически простыми. Если реальных примеров мало, результат по таким отказам обычно менее стабильный.

`StratifiedKFold` дает более спокойную картину, потому что данные перемешиваются. `TimeSeriesSplit` строже: обучение идет на прошлом, проверка на будущем. Для временных данных такая проверка полезнее.

`PCA` нужен для просмотра структуры данных. Если классы на 2D-графике пересекаются, значит простого визуального разделения нет и нужна модель по исходным признакам.
